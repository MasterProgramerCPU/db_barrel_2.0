#!/bin/bash
set -e

# Stop the service before removal
if systemctl is-active --quiet db-barrel.service 2>/dev/null; then
    systemctl stop db-barrel.service
fi

systemctl disable db-barrel.service 2>/dev/null || true
